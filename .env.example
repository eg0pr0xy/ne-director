DIRECTOR_DATABASE_URL=postgres://ne_director:ne_director_local_only@127.0.0.1:55434/ne_director
# Ingress provider secrets are intentionally absent. Do not add real mail or
# calendar credentials here; inject them only through the approved local secret
# authority after backing-provider discovery and operator authorization.
DIRECTOR_PORT=4600
VITE_DIRECTOR_RUNTIME_MODE=api
VITE_DIRECTOR_API_BASE_URL=http://127.0.0.1:4600/api/v1
