DIRECTOR_DATABASE_URL=postgres://ne_director:ne_director_local_only@127.0.0.1:55434/ne_director
# Google Provider 001 needs a local secret authority, never values in this file:
# DIRECTOR_GOOGLE_OAUTH_CLIENT_ID, DIRECTOR_GOOGLE_OAUTH_CLIENT_SECRET,
# DIRECTOR_GOOGLE_OAUTH_REDIRECT_URI, and DIRECTOR_GOOGLE_REFRESH_TOKEN (or a
# connection-specific DIRECTOR_* reference). Do not add real mail or calendar
# credentials here; inject them only through the approved local secret authority.
DIRECTOR_PORT=4600
VITE_DIRECTOR_RUNTIME_MODE=api
VITE_DIRECTOR_API_BASE_URL=http://127.0.0.1:4600/api/v1
